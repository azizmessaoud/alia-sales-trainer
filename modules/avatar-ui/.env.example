# ALIA avatar-ui module — 3D avatar rendering and animation
# Only variables needed for standalone avatar component testing.

# Optional: Avatar rendering toggles
AVATAR_ENABLE_PROFILER=false
AVATAR_DEBUG_BLENDSHAPES=false
NODE_ENV=development
